{

{-f1 0 = 1;
f1 n = n+2;

f2 = 1+1;-}

{-
um = 1; -- constant
um2 = 0+1; -- class um2, method apply = Integer apply() { return 0+1; }
dois = 2.0;

asda = 1+1;
-}


--soma_um y = (y + 1);
-- soma x y = x+y;

-- class soma_umA1, method apply = Integer apply(Integer x) { return x+(new Um2()).apply(); }

{-
map f [] = [];
map f (x:xs) = f x : map f xs;

soma2 x = x+2;
soma2lista = map soma2;
-}

-- soma_um y = 1+y;

--empty_list = [];
-- one_item = 1:[];
two_items = [1,2];

{-
Right ([],[(blah,[([],Ap (Ap (Var +) (Lit (LitInt 2))) (Lit (LitInt 1)))])])
blah = (2+1)
-}

}